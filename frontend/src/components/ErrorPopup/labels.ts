import type { ErrorPopupLabels } from './types.ts';

/** Tous les textes par défaut affichés par le composant. */
export const defaultLabels: ErrorPopupLabels = {
  title: 'Une erreur est survenue',
  close: 'Fermer',
  retry: 'Réessayer',
};
