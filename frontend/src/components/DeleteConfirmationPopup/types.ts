/** Types de libellés du DeleteConfirmationPopup. */

/**
 * Tous les textes affichés par le composant.
 * Passés via la prop `labels` (en Partial) ; les champs omis prennent les défauts.
 */
export interface DeleteConfirmationPopupLabels {
  /** Bouton « annuler ». */
  cancel: string;
  /** Bouton de confirmation de suppression. */
  confirm: string;
}
