info:
  name: Forum · Supprimer le sujet
  type: http
  seq: 7

http:
  method: DELETE
  url: http://localhost:8081/dev/ws/post?forumId=3&postId=102

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  Diffuse la suppression du sujet <postId> sur forum:<forumId>.
