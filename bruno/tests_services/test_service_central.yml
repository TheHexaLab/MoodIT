info:
  name: test_service_central
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8081/api/test

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
