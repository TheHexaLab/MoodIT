info:
  name: Message · Modifier
  type: http
  seq: 3

http:
  method: PATCH
  url: http://localhost:8081/dev/ws/message?channelId=1&messageId=1&content=(dev) message modifié ✏️

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  Diffuse la modification du message <messageId> sur channel:<channelId>.
  messageId doit exister dans la room ouverte côté client (mock : 1..7).
