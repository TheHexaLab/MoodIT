info:
  name: POST/establishments
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/establishments"
  body:
    type: json
    data: |-
      {
        "name": "mimimi",
        "domainEmail": "mimi.ca"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
