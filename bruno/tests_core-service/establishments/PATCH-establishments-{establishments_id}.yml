info:
  name: PATCH/establishments/{establishments_id}
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/establishments/2"
  body:
    type: json
    data: |-
      {
        "name": "Polytechnique Montréal patche",
        "domainEmail": "polymtlpatch.ca"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
