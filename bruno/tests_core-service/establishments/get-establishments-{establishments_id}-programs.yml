info:
  name: get/establishments/{establishments_id}/programs
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/establishments/1/programs"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
