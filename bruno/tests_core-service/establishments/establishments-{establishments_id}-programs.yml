info:
  name: establishments/{establishments_id}/programs
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/establishments/programs"
  body:
    type: json
    data: |-
      {
        "establishmentId": 1,
        "name": "Génie abc2",
        "code": "ABC",
        "cohort": "71",
        "color": "#ff8800"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
