info:
  name: /programs/{programId}/users
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/programs/1/users"
  headers:
    - name: X-User-Email
      value: dufl8605@usherbrooke.ca
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
