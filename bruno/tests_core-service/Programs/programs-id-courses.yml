info:
  name: /programs/id/courses
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/programs/2/courses"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
