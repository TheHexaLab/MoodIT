info:
  name: /programs/id/courses/id
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/programs/2/courses/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
