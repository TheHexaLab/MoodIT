info:
  name: /programs/users
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/programs/users"
  body:
    type: json
    data: |-
      {
        "id": 1,
        "programIds":[5, 6, 4]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
