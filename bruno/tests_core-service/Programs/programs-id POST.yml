info:
  name: /programs/id POST
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/programs/7"
  body:
    type: json
    data: |-
      {
        "code": "GENN"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
