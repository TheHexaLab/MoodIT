info:
  name: /programs/id
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/programs/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
