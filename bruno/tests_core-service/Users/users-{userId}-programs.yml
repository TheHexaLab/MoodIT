info:
  name: /users/{userId}/programs
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/users/44/programs"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
