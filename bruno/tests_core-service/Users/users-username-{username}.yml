info:
  name: /users/username/{username}
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/users/username/rosie1234"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
