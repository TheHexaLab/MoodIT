info:
  name: /users/role/{role}
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/users/role/Étudiant"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
