info:
  name: /users/2/programs/2/enrollments
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/users/2/programs/1/enrollments"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
