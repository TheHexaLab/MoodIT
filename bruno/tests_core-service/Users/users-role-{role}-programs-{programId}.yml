info:
  name: /users/role/{role}/programs/{programId}
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/users/role/1/programs/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
