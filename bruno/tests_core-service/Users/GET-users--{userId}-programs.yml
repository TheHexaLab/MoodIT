info:
  name: GET/users//{userId}/programs
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/users/2/programs"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
