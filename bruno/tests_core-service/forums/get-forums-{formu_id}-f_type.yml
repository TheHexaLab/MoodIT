info:
  name: get/forums/{formu_id}/f_type
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/forums/{formu_id}/f_type"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
