info:
  name: /forums/id
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/forums/2"
  body:
    type: json
    data: |-
      {
        "title" : "Discussions — Mathématiques pour ingénieurs II"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
