info:
  name: /forums/id/posts/id
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/forums/1/posts/1?loadChildren=true"
  params:
    - name: loadChildren
      value: "true"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
