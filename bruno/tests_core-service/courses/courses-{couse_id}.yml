info:
  name: /courses/{couse_id}
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/courses/24"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
