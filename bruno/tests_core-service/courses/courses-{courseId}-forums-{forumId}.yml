info:
  name: /courses/{courseId}/forums/{forumId}
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/courses/1/forums/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
