info:
  name: DELETE/courses/{course_id}
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/courses/24"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
