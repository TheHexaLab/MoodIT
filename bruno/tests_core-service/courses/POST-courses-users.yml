info:
  name: POST/courses/users
  type: http
  seq: 8

http:
  method: POST
  url: "{{baseUrl}}/courses/users"
  body:
    type: json
    data: |-
      {
        "id": 2,
        "courseIds": [1, 2]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
