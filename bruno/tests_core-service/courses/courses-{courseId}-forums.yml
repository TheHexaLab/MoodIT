info:
  name: /courses/{courseId}/forums
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/courses/1/forums"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
