info:
  name: Patch/courses/{course_id}
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/courses/1"
  body:
    type: json
    data: |-
      {
        "title": "Mathématiques pour ingénieurs I + patch",
        "code": "MAP115"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
