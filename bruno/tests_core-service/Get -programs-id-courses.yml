info:
  name: Get /programs/id/courses
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/programs/GIN/courses"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
